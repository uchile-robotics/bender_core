<launch>

	<arg name="respawn" default="false"/>

	<!-- RVIZ -->
	<node name="$(anon rviz)" pkg="rviz" type="rviz"
	 respawn="$(arg respawn)" output="screen" args="-d $(find uchile_util)/config/rviz/common.rviz"/>

</launch>