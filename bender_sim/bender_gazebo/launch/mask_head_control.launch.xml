
<launch>
  <!-- Load head controller configurations -->
  <rosparam file="$(find bender_gazebo)/config/mask_head_control.yaml" command="load"/>
  <!-- Load the controllers, launch in other console -->
  <node name="neck_controller_spawner" pkg="controller_manager" type="spawner" respawn="false" output="screen" ns="/bender" args="head_controller"/>
</launch>
